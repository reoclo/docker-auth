import * as core from "@actions/core";

async function run(): Promise<void> {
  core.info("docker-auth main step: scaffold stub — implementation lands in M3");
  core.setFailed("docker-auth action is not yet implemented");
}

run();
